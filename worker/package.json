{
  "name": "hokkaido-anthropic-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker proxy for the Hokkaido Travel Note site so the Anthropic API key never reaches the browser.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "secret": "wrangler secret put ANTHROPIC_API_KEY",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.78.0"
  }
}
